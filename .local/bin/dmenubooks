#!/bin/sh

loc="/mnt/EXTRA/Books/"
books="$(cd $loc && ls */**)"
declare options=("$books")

choice=$(echo -e "${options[@]}"| dmenu -i -p "Select a book: " -l 15)
[ -z "$choice" ] && exit

zathura "$loc$choice"
